<%= form_for @post do |form| %>
  <div>
    <%= form.label :message, "Edit what you want to tell the world..", class: "new-post-label"%>
  </div>
  <div>
  <%= form.text_area :message, size: "50%x3" %>
  </div>
  <div>
  <%= form.submit "Submit", class: "new-post-submit" %>
  </div>
<% end %>